
```catala-metadata
declaration sort_assoc
  content list of (integer, anything of type t)
  depends on l content list of (integer, anything of type t)
  equals
    let (result, remaining) equals
      combine all bnd among l in (acc, rem) initially ([], l)
      with
          let elt equals content of (i, x) among rem such that i is minimum in
          (acc ++ [ elt ],
           list of bnd among rem such that bnd != elt)
    in
    result
```


```catala
#[test]
declaration scope Test:
  output r content list of (integer, money)

scope Test:
  definition r equals
  sort_assoc of [(1, $3); (44, $2); (2, $1); (3, $2); (-1, $5); (100, $100); (-2, $10)]
```


```catala-test-cli
$ catala test-scope Test --debug
┌─[ERROR]─
│
│  Internal Error, please report to https://github.com/CatalaLang/catala/issues.
│
│  function has 2 variables but was supplied 1 types
λ (i: <any type>) →
│  [ERROR] Invalid_argument("List.map2")
#return code 2#
```

