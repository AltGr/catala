## Test

```catala
declaration scope A:
  context f content integer depends on integer

declaration scope B:
  input b content boolean
  a scope A

scope A:
  definition f of x equals x + 1

scope B:
  definition a.f of x under condition b and x > 0 consequence equals x - 1
```

```catala-test-inline
$ catala Scopelang -s B
[ERROR] Syntax error at token "integer"
Message: unexpected token
Autosuggestion: did you mean "("?

Error token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.39-46:
└─┐
5 │   context f content integer depends on integer
  │                                        ‾‾‾‾‾‾‾


Last good token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.28-38:
└─┐
5 │   context f content integer depends on integer
  │                             ‾‾‾‾‾‾‾‾‾‾

#return code 255#
```

```catala-test-inline
$ catala Dcalc -s A
[ERROR] Syntax error at token "integer"
Message: unexpected token
Autosuggestion: did you mean "("?

Error token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.39-46:
└─┐
5 │   context f content integer depends on integer
  │                                        ‾‾‾‾‾‾‾


Last good token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.28-38:
└─┐
5 │   context f content integer depends on integer
  │                             ‾‾‾‾‾‾‾‾‾‾

#return code 255#
```

```catala-test-inline
$ catala Dcalc -s B
[ERROR] Syntax error at token "integer"
Message: unexpected token
Autosuggestion: did you mean "("?

Error token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.39-46:
└─┐
5 │   context f content integer depends on integer
  │                                        ‾‾‾‾‾‾‾


Last good token:
┌─⯈ tests/test_func/good/context_func.catala_en:5.28-38:
└─┐
5 │   context f content integer depends on integer
  │                             ‾‾‾‾‾‾‾‾‾‾

#return code 255#
```
