## Article

```catala
declaration scope RecursiveFunc:
  context f content integer depends on integer

scope RecursiveFunc:
  definition f of x equals f of x + 1
```

```catala-test-inline
$ catala Interpret -s RecursiveFunc
[ERROR] Syntax error at token "integer"
Message: unexpected token
Autosuggestion: did you mean "("?

Error token:
┌─⯈ tests/test_func/bad/recursive.catala_en:5.39-46:
└─┐
5 │   context f content integer depends on integer
  │                                        ‾‾‾‾‾‾‾


Last good token:
┌─⯈ tests/test_func/bad/recursive.catala_en:5.28-38:
└─┐
5 │   context f content integer depends on integer
  │                             ‾‾‾‾‾‾‾‾‾‾

#return code 255#
```
